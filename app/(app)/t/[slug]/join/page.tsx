import { IntakeScreen } from '@/components/intake/intake-screen';

/** Screen 02 — Preference Intake, from a joining member's phone. */
export default function JoinPage() {
  return <IntakeScreen />;
}
